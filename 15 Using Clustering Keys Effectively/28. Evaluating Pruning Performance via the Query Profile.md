# Evaluating Pruning Performance via the Query Profile

**Description:**
This topic explains how to evaluate the performance of pruning in Snowflake using the Query Profile. Pruning is a technique used to improve query performance by reducing the amount of data that needs to be scanned. The Query Profile provides detailed information about query execution, including pruning performance.

## Short Explanation
**Overview:** Evaluating pruning performance in Snowflake using the Query Profile.

**Problem Solved:** Identifying and optimizing pruning performance issues in Snowflake queries.

**Importance:** Improving query performance and reducing costs by optimizing pruning.

**Use Cases:**
- Evaluating pruning performance for a specific query
- Identifying pruning performance issues in a Snowflake warehouse

### Typical Scenario
A data analyst wants to evaluate the performance of a query that uses pruning to improve performance. They use the Query Profile to analyze the pruning performance and identify potential optimization opportunities.

### Core Snowflake SQL Objects Used


### Code Source Execution
```sql
-- no snippet mapped
```

### Example Execution Logic Step-by-Step
To evaluate pruning performance via the Query Profile, follow these steps:
1. Execute a query that uses pruning.
2. Go to the Snowflake web interface and navigate to the Query Profile page.
3. Select the query you want to analyze.
4. In the Query Profile page, look for the 'Pruning' section.
5. Analyze the pruning performance metrics, such as 'Pruning ratio' and 'Bytes saved by pruning'.

### Implementation Example


### Explanation of the Code


### When to Use
- When evaluating pruning performance for a specific query
- When identifying pruning performance issues in a Snowflake warehouse

### When Not to Use
- When pruning is not used in the query
- When the Query Profile is not available

### Common Mistakes
- Not analyzing pruning performance metrics
- Not optimizing pruning performance issues

### Expected Output
**Description:** The Query Profile page shows detailed information about pruning performance, including metrics such as 'Pruning ratio' and 'Bytes saved by pruning'.

**Schema:**
- Pruning ratio
- Bytes saved by pruning

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*