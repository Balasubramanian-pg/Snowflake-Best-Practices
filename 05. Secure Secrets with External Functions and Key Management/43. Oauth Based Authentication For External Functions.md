# Dynamic Data Masking in Snowflake

**Description:**
Dynamic Data Masking (DDM) is a security feature in Snowflake that allows administrators to limit sensitive data exposure by masking it to unauthorized users. DDM applies masks to data at query time, ensuring that users only see authorized data. This feature helps protect sensitive data while maintaining data usability.

## Short Explanation
**Overview:** Dynamic Data Masking (DDM) is a feature that masks sensitive data at query time.

**Problem Solved:** DDM solves the problem of unauthorized data access by masking sensitive information.

**Importance:** DDM is crucial in analytics and warehousing for protecting sensitive data.

**Use Cases:**
- Securing customer PII data\n- Protecting financial information

### Typical Scenario
A company wants to provide analysts with access to customer data while ensuring that sensitive information like social security numbers and credit card numbers are not exposed.

### Core Snowflake SQL Objects Used
`CREATE MASKING POLICY`, `ALTER TABLE`

### Code Source Execution
```sql
CREATE MASKING POLICY mask_sensitive_data AS (val string) RETURNS string -> CASE WHEN CURRENT_ROLE() IN ('ANALYST') THEN val ELSE 'XXXX' END;
```

### Example Execution Logic Step-by-Step
To implement DDM, first create a masking policy using 'CREATE MASKING POLICY'. Then, apply the policy to a table column using 'ALTER TABLE ... MODIFY COLUMN ... SET MASKING POLICY'.

### Implementation Example
CREATE TABLE customers (id INT, ssn STRING); CREATE MASKING POLICY mask_ssn AS (val string) RETURNS string -> CASE WHEN CURRENT_ROLE() IN ('ANALYST') THEN val ELSE 'XXXX' END; ALTER TABLE customers MODIFY COLUMN ssn SET MASKING POLICY mask_ssn;

### Explanation of the Code
- Create a table with a sensitive column (e.g., ssn).\n- Create a masking policy that masks the sensitive data based on the current role.\n- Apply the masking policy to the sensitive column.

### When to Use
- When you need to protect sensitive data from unauthorized access\n- When you want to limit data exposure to certain roles

### When Not to Use
- When all users are trusted and have the necessary permissions\n- When data is already encrypted and access is strictly controlled

### Common Mistakes
- Forgetting to apply the masking policy to all relevant columns\n- Not testing the masking policy thoroughly

### Expected Output
**Description:** The result set will show masked data for unauthorized users.

**Schema:**
- id\n- ssn

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*