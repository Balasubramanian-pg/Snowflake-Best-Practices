# In-Flight Query Consumption Impact on Threshold Accuracy in Snowflake

**Description:**
This topic discusses how in-flight query consumption affects threshold accuracy in Snowflake, particularly in the context of resource monitors and quota policies. It highlights the importance of considering in-flight query consumption when setting thresholds to avoid unexpected behavior. By understanding this impact, users can optimize their threshold settings for more accurate monitoring and alerting.

## Short Explanation
**Overview:** In-flight query consumption can significantly impact threshold accuracy in Snowflake.

**Problem Solved:** Avoiding unexpected behavior in resource monitors and quota policies due to in-flight query consumption.

**Importance:** Ensures accurate monitoring and alerting in Snowflake.

**Use Cases:**
- Setting up resource monitors with accurate thresholds
- Optimizing quota policies for in-flight queries

### Typical Scenario
A Snowflake user sets up a resource monitor with a threshold of 80% credit usage. However, due to in-flight queries, the actual credit usage exceeds the threshold, triggering an alert. The user must then adjust the threshold to account for in-flight query consumption.

### Core Snowflake SQL Objects Used
`CREATE RESOURCE MONITOR`, `ALTER RESOURCE MONITOR`

### Code Source Execution
```sql
CREATE RESOURCE MONITOR rm_monitor
  WAREHOUSE = 'wh_warehouse'
  CREDIT QUOTA = 100
  THRESHOLD = 80 PERCENT;

ALTER RESOURCE MONITOR rm_monitor
  SET THRESHOLD = 90 PERCENT;
```

### Example Execution Logic Step-by-Step
In this example, the resource monitor 'rm_monitor' is created with an initial threshold of 80%. To account for in-flight query consumption, the threshold is adjusted to 90% using the ALTER RESOURCE MONITOR statement.

### Implementation Example
To implement this, users can create a resource monitor with a threshold that takes into account in-flight query consumption. For example:

CREATE RESOURCE MONITOR rm_monitor
  WAREHOUSE = 'wh_warehouse'
  CREDIT QUOTA = 100
  THRESHOLD = 90 PERCENT;

Users can then adjust the threshold as needed based on actual usage patterns.

### Explanation of the Code
- Step 1: Create a resource monitor with an initial threshold.
- Step 2: Adjust the threshold to account for in-flight query consumption.

### When to Use
- When setting up resource monitors with accurate thresholds
- When optimizing quota policies for in-flight queries

### When Not to Use
- When in-flight query consumption is negligible
- When using fixed thresholds that do not account for usage variability

### Common Mistakes
- Underestimating in-flight query consumption
- Failing to adjust thresholds for accurate monitoring

### Expected Output
**Description:** The expected output is a resource monitor with an adjusted threshold that accurately reflects in-flight query consumption.

**Schema:**
- Resource Monitor Name
- Warehouse
- Credit Quota
- Threshold

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*