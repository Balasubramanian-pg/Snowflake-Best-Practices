# Dynamic Data Masking in Snowflake

**Description:**
Dynamic Data Masking (DDM) is a security feature in Snowflake that allows administrators to limit sensitive data exposure by masking it to unauthorized users. DDM applies masks to data at query time, ensuring that users only see authorized data. This feature helps prevent data breaches and ensures compliance with data protection regulations.

## Short Explanation
**Overview:** Dynamic Data Masking (DDM) is a feature that masks sensitive data at query time.

**Problem Solved:** DDM solves the problem of sensitive data exposure to unauthorized users.

**Importance:** DDM is crucial in analytics and warehousing for data security and compliance.

**Use Cases:**
- Securing customer PII data\n- Protecting financial information

### Typical Scenario
A company wants to allow data analysts to query customer data without exposing sensitive information like social security numbers or credit card numbers.

### Core Snowflake SQL Objects Used
`CREATE MASKING POLICY`, `ALTER TABLE`

### Code Source Execution
```sql
CREATE MASKING POLICY mask_sensitive_data AS (val string) RETURNS string -> CASE WHEN CURRENT_ROLE() = 'ANALYST' THEN val ELSE 'XXXX' END;
```

### Example Execution Logic Step-by-Step
To implement DDM, first create a masking policy using CREATE MASKING POLICY. Then, apply the policy to a table column using ALTER TABLE.

### Implementation Example
CREATE TABLE customers (id INT, ssn STRING); CREATE MASKING POLICY mask_ssn AS (val string) RETURNS string -> 'XXXX'; ALTER TABLE customers MODIFY COLUMN ssn SET MASKING POLICY mask_ssn;

### Explanation of the Code
- Create a masking policy that returns 'XXXX' for any value if the current role is not 'ANALYST'.\n- Apply the masking policy to a table column.

### When to Use
- When you need to protect sensitive data from unauthorized access\n- When you need to comply with data protection regulations

### When Not to Use
- When performance is critical and masking overhead is a concern\n- When data is already encrypted

### Common Mistakes
- Forgetting to grant the masking policy to the correct roles\n- Not testing the masking policy thoroughly

### Expected Output
**Description:** The result set will show masked data for unauthorized users.

**Schema:**
- id\n- ssn

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*