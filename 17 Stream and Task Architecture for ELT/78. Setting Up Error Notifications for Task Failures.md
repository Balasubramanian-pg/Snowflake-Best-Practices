# Setting Up Error Notifications for Task Failures in Snowflake

**Description:**
This topic explains how to configure error notifications for task failures in Snowflake, ensuring that users are informed when tasks fail, allowing for prompt investigation and resolution. Error notifications can be set up using Snowflake's notification integration with external services. This helps in maintaining the reliability and robustness of data pipelines.

## Short Explanation
**Overview:** Configuring error notifications for task failures in Snowflake.

**Problem Solved:** Notifying users of task failures to ensure prompt action.

**Importance:** Enhances data pipeline reliability and maintainability.

**Use Cases:**
- Data pipeline monitoring
- Task failure alerting

### Typical Scenario
A data engineer wants to set up notifications for task failures in a Snowflake data pipeline. The engineer creates a notification integration with an external service, such as Slack or PagerDuty, and configures a task to send notifications on failure.

### Core Snowflake SQL Objects Used
`CREATE TASK`, `ALTER TASK`

### Code Source Execution
```sql
CREATE TASK my_task
  WAREHOUSE = 'my_warehouse'
  SCHEDULE = '0 12 * * *'
  COMMENT = 'My task'
  CALL my_stored_procedure();

ALTER TASK my_task
  SET
    ON_FAILURE = 'CALL my_error_handler()';
```

### Example Execution Logic Step-by-Step
To set up error notifications for task failures, follow these steps:
1. Create a notification integration with an external service.
2. Create a stored procedure to handle error notifications.
3. Create a task and specify the stored procedure to be called on failure.

### Implementation Example
CREATE NOTIFICATION INTEGRATION my_notification_integration
  TYPE = ' slack'
  SLACK_CHANNEL = '#my_channel'
  SLACK_USERNAME = 'Snowflake';

CREATE STORED PROCEDURE my_error_handler()
  RETURNS STRING
  LANGUAGE JAVASCRIPT
  EXECUTE AS CALLER
  RETURNS NULL ON NULL INPUT
  COMMENT = 'Handles task failure notifications'
  function main() {
    // Send notification to Slack
  }

CREATE TASK my_task
  WAREHOUSE = 'my_warehouse'
  SCHEDULE = '0 12 * * *'
  COMMENT = 'My task'
  CALL my_stored_procedure();

ALTER TASK my_task
  SET
    ON_FAILURE = 'CALL my_error_handler()';

### Explanation of the Code
- Step 1: Create a notification integration with an external service.
- Step 2: Create a stored procedure to handle error notifications.
- Step 3: Create a task and specify the stored procedure to be called on failure.

### When to Use
- Data pipeline monitoring
- Task failure alerting

### When Not to Use
- For tasks that do not require notifications
- For tasks with low failure rates

### Common Mistakes
- Not configuring notifications for critical tasks
- Using incorrect notification integration settings

### Expected Output
**Description:** The task failure notification is sent to the specified external service.

**Schema:**
- Notification message
- Task name
- Failure reason

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*