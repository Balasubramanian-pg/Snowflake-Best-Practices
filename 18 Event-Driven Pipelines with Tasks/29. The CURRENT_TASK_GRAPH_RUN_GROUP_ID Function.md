# The CURRENT_TASK_GRAPH_RUN_GROUP_ID Function in Snowflake

**Description:**
The CURRENT_TASK_GRAPH_RUN_GROUP_ID function in Snowflake returns the ID of the current task graph run group. This function is useful in event-driven pipelines with tasks, allowing users to track and manage task executions.

## Short Explanation
**Overview:** The CURRENT_TASK_GRAPH_RUN_GROUP_ID function returns the ID of the current task graph run group in Snowflake.

**Problem Solved:** This function solves the problem of tracking and managing task executions in event-driven pipelines.

**Importance:** It is important in analytics and warehousing as it allows users to monitor and control task executions.

**Use Cases:**
- Tracking task executions in event-driven pipelines
- Managing task dependencies and workflows

### Typical Scenario
A typical scenario where this function is used is in an event-driven pipeline with multiple tasks, where users need to track the execution of tasks and manage dependencies between them.

### Core Snowflake SQL Objects Used


### Code Source Execution
```sql
SELECT CURRENT_TASK_GRAPH_RUN_GROUP_ID();
```

### Example Execution Logic Step-by-Step
The CURRENT_TASK_GRAPH_RUN_GROUP_ID function can be used in a SQL query to retrieve the ID of the current task graph run group. For example, in a stored procedure or a task execution script.

### Implementation Example
CREATE OR REPLACE PROCEDURE example_procedure()
  RETURNS VARIANT
  LANGUAGE JAVASCRIPT
  EXECUTE AS CALLER
  RETURNS NULL ON NULL INPUT
  COMMENT = 'Example procedure demonstrating CURRENT_TASK_GRAPH_RUN_GROUP_ID'
  AS
  $$
  var sql = "SELECT CURRENT_TASK_GRAPH_RUN_GROUP_ID();";
  var stmt = snowflake.createStatement({sql: sql});
  var rs = stmt.execute();
  rs.next();
  var taskGraphRunGroupId = rs.getColumnValue(1);
  return taskGraphRunGroupId;
  $$;

### Explanation of the Code
- Step 1: Create a stored procedure with a SQL statement that calls the CURRENT_TASK_GRAPH_RUN_GROUP_ID function.
- Step 2: Execute the stored procedure to retrieve the ID of the current task graph run group.

### When to Use
- Use in event-driven pipelines with tasks to track task executions.
- Use in stored procedures or scripts to manage task dependencies and workflows.

### When Not to Use
- Do not use in queries that do not require task graph run group ID tracking.
- Avoid using in scenarios where task executions are not dependent on each other.

### Common Mistakes
- Forgetting to handle the result of the CURRENT_TASK_GRAPH_RUN_GROUP_ID function.
- Not properly managing task dependencies and workflows.

### Expected Output
**Description:** The result of the CURRENT_TASK_GRAPH_RUN_GROUP_ID function is the ID of the current task graph run group.

**Schema:**
- CURRENT_TASK_GRAPH_RUN_GROUP_ID

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*