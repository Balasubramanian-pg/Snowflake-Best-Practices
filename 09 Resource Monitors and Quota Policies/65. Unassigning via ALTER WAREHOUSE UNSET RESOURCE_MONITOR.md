# Unassigning a Resource Monitor from a Warehouse using ALTER WAREHOUSE UNSET RESOURCE_MONITOR

**Description:**
This topic explains how to unassign a resource monitor from a warehouse in Snowflake using the ALTER WAREHOUSE UNSET RESOURCE_MONITOR command. This command allows you to remove a resource monitor that is currently associated with a warehouse. By unassigning a resource monitor, you can change the monitoring and alerting configuration for a warehouse.

## Short Explanation
**Overview:** Unassigning a resource monitor from a warehouse in Snowflake.

**Problem Solved:** Removing a resource monitor that is currently associated with a warehouse.

**Importance:** Allows for flexible monitoring and alerting configuration for warehouses.

**Use Cases:**
- Changing the monitoring configuration for a warehouse
- Removing alerting for a specific warehouse

### Typical Scenario
A business scenario where a company wants to change the monitoring configuration for one of its warehouses. For example, a company may want to remove the resource monitor associated with a development warehouse because it is no longer needed.

### Core Snowflake SQL Objects Used
`ALTER WAREHOUSE`

### Code Source Execution
```sql
ALTER WAREHOUSE my_warehouse UNSET RESOURCE_MONITOR my_resource_monitor;
```

### Example Execution Logic Step-by-Step
To unassign a resource monitor from a warehouse, you can use the ALTER WAREHOUSE command with the UNSET RESOURCE_MONITOR option. This command takes two parts: the name of the warehouse and the name of the resource monitor to unassign.

### Implementation Example
Suppose we have a warehouse named 'my_warehouse' and a resource monitor named 'my_resource_monitor'. To unassign the resource monitor from the warehouse, we can execute the following SQL command: ALTER WAREHOUSE my_warehouse UNSET RESOURCE_MONITOR my_resource_monitor;

### Explanation of the Code
- Specify the warehouse name: 'my_warehouse'
- Specify the UNSET RESOURCE_MONITOR option
- Specify the resource monitor name: 'my_resource_monitor'

### When to Use
- When you want to change the monitoring configuration for a warehouse
- When you want to remove alerting for a specific warehouse

### When Not to Use
- When you want to assign a new resource monitor to a warehouse (use ALTER WAREHOUSE SET RESOURCE_MONITOR instead)
- When you want to create a new warehouse (use CREATE WAREHOUSE instead)

### Common Mistakes
- Forgetting to specify the warehouse or resource monitor name
- Using the wrong SQL command (e.g., SET instead of UNSET)

### Expected Output
**Description:** The command will execute successfully if the resource monitor is unassigned from the warehouse. You can verify the change by querying the information schema.

**Schema:**
- WAREHOUSE_NAME
- RESOURCE_MONITOR_NAME

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*