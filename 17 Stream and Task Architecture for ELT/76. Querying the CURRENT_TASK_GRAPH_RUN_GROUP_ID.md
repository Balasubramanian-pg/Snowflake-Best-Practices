# Querying the CURRENT_TASK_GRAPH_RUN_GROUP_ID in Snowflake

**Description:**
This topic explains how to query the CURRENT_TASK_GRAPH_RUN_GROUP_ID in Snowflake, which is used to retrieve the ID of the current task graph run group. This allows users to track and monitor task executions within a specific graph run.

## Short Explanation
**Overview:** The CURRENT_TASK_GRAPH_RUN_GROUP_ID function returns the ID of the current task graph run group in Snowflake.

**Problem Solved:** This function helps users to identify and track task executions within a specific graph run, making it easier to manage and debug task workflows.

**Importance:** Understanding the CURRENT_TASK_GRAPH_RUN_GROUP_ID is crucial for managing complex task workflows and ensuring data consistency in Snowflake.

**Use Cases:**
- Monitoring task executions
- Debugging task workflows
- Auditing task history

### Typical Scenario
A data engineer wants to track the execution of a complex task workflow that involves multiple tasks and dependencies. They can use the CURRENT_TASK_GRAPH_RUN_GROUP_ID function to retrieve the ID of the current task graph run group and then query the task history to monitor the execution of the tasks.

### Core Snowflake SQL Objects Used
`SELECT CURRENT_TASK_GRAPH_RUN_GROUP_ID()`

### Code Source Execution
```sql
SELECT CURRENT_TASK_GRAPH_RUN_GROUP_ID();
```

### Example Execution Logic Step-by-Step
The CURRENT_TASK_GRAPH_RUN_GROUP_ID function returns a unique ID that identifies the current task graph run group. This ID can be used to query the task history and retrieve information about the tasks that are executing within the graph run.

### Implementation Example
CREATE TABLE task_history AS
SELECT CURRENT_TASK_GRAPH_RUN_GROUP_ID() AS task_graph_run_group_id, *
FROM TABLE(information_schema.task_history());

### Explanation of the Code
- Step 1: Use the CURRENT_TASK_GRAPH_RUN_GROUP_ID function to retrieve the ID of the current task graph run group.
- Step 2: Query the task history table using the retrieved ID to get information about the tasks that are executing within the graph run.

### When to Use
- When monitoring task executions
- When debugging task workflows
- When auditing task history

### When Not to Use
- When the task graph run group ID is not required
- When querying task history for a specific task

### Common Mistakes
- Not checking the data type of the task graph run group ID
- Not handling errors when querying task history

### Expected Output
**Description:** The result set will contain the ID of the current task graph run group.

**Schema:**
- task_graph_run_group_id

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*