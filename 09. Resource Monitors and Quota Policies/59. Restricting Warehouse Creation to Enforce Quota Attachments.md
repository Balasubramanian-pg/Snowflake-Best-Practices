# Restricting Warehouse Creation to Enforce Quota Attachments in Snowflake

**Description:**
This topic explains how to restrict warehouse creation in Snowflake to enforce quota attachments, ensuring that warehouses are created with the correct quota policies attached. This is achieved through a combination of Snowflake's resource monitor and quota policy features. By implementing this approach, organizations can better manage their Snowflake resources and prevent unexpected costs.

## Short Explanation
**Overview:** Restricting warehouse creation to enforce quota attachments helps organizations manage their Snowflake resources effectively.

**Problem Solved:** Prevents warehouses from being created without the correct quota policies attached, which can lead to unexpected costs.

**Importance:** Ensures that Snowflake resources are utilized within the defined quota limits, preventing unexpected costs and optimizing resource allocation.

**Use Cases:**
- Implementing quota policies for different departments or teams within an organization
- Enforcing resource utilization limits for specific Snowflake roles or users

### Typical Scenario
A large organization with multiple departments wants to ensure that each department's Snowflake usage is within the allocated quota limits. They create a resource monitor and quota policy to track and enforce usage limits for each department.

### Core Snowflake SQL Objects Used
`CREATE RESOURCE MONITOR`, `CREATE QUOTA POLICY`

### Code Source Execution
```sql
CREATE RESOURCE MONITOR my_resource_monitor
  WITH (CPU = 10, memory = 100);

CREATE QUOTA POLICY my_quota_policy
  ON RESOURCE MONITOR my_resource_monitor
  WITH (quota = 1000);
```

### Example Execution Logic Step-by-Step
To restrict warehouse creation and enforce quota attachments, you create a resource monitor to track usage and a quota policy to define the limits. You then attach the quota policy to the resource monitor. When a warehouse is created, it must be attached to the resource monitor with the quota policy.

### Implementation Example
To implement this approach, you can create a stored procedure that checks if a warehouse has a quota policy attached before creating it. If not, it raises an error.

### Explanation of the Code
- Step 1: Create a resource monitor to track usage
- Step 2: Create a quota policy to define the limits
- Step 3: Attach the quota policy to the resource monitor

### When to Use
- When you want to enforce quota limits for specific Snowflake roles or users
- When you want to prevent unexpected costs due to uncontrolled Snowflake usage

### When Not to Use
- When you have a simple Snowflake usage scenario with no quota limits
- When you are not concerned about unexpected costs due to Snowflake usage

### Common Mistakes
- Forgetting to attach the quota policy to the resource monitor
- Not defining the correct quota limits for your organization

### Expected Output
**Description:** When a warehouse is created with a quota policy attached, Snowflake displays the quota policy details in the warehouse properties.

**Schema:**
- Warehouse Name
- Quota Policy Name
- Quota Limit

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*