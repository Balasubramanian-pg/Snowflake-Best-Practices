# Identifying Non-Compliant Object Names in Snowflake via Information Schema

**Description:**
This topic explains how to identify object names in Snowflake that do not comply with your organization's naming conventions. It leverages the Information Schema to query and report on non-compliant object names.

## Short Explanation
**Overview:** Using Snowflake's Information Schema to identify object names that do not follow established naming conventions.

**Problem Solved:** Easily finding and rectifying non-compliant object names to maintain a clean and organized Snowflake environment.

**Importance:** Ensures data organization and simplifies maintenance and governance.

**Use Cases:**
- Auditing existing object names for compliance with new naming standards
- Regularly monitoring object creation for adherence to naming conventions

### Typical Scenario
A Snowflake administrator needs to ensure all tables, views, and schemas follow a specific naming convention (e.g., prefix for test vs. production environments). They use the Information Schema to identify non-compliant object names.

### Core Snowflake SQL Objects Used
`INFORMATION_SCHEMA.TABLES`, `INFORMATION_SCHEMA.VIEWS`, `INFORMATION_SCHEMA.SCHEMAS`

### Code Source Execution
```sql
```sql
SELECT 
    TABLE_NAME
FROM 
    INFORMATION_SCHEMA.TABLES
WHERE 
    TABLE_NAME NOT LIKE 'prod_%';
```
```

### Example Execution Logic Step-by-Step
This SQL query selects table names from the Information Schema that do not start with 'prod_', indicating they are not compliant with a naming convention requiring production tables to have this prefix.

### Implementation Example
To implement a comprehensive check for non-compliant object names across different types of objects (tables, views, schemas), you could use a script like this:
```sql
-- Non-compliant table names
SELECT 
    TABLE_NAME
FROM 
    INFORMATION_SCHEMA.TABLES
WHERE 
    TABLE_NAME NOT LIKE 'prod_%';

-- Non-compliant view names
SELECT 
    TABLE_NAME
FROM 
    INFORMATION_SCHEMA.VIEWS
WHERE 
    TABLE_NAME NOT LIKE 'prod_%';

-- Non-compliant schema names
SELECT 
    SCHEMA_NAME
FROM 
    INFORMATION_SCHEMA.SCHEMATA
WHERE 
    SCHEMA_NAME NOT LIKE 'prod_%';
```

### Explanation of the Code
- The query uses Snowflake's Information Schema to access metadata about tables, views, and schemas.
- The WHERE clause filters object names based on a specific pattern. For example, 'prod_%' is used to identify production objects.

### When to Use
- During the initial setup of a Snowflake environment to enforce naming conventions.
- Regularly to monitor and maintain compliance with evolving standards.

### When Not to Use
- When there are no established naming conventions.
- In environments where object names are dynamically generated and cannot follow a standard pattern.

### Common Mistakes
- Not regularly updating scripts to reflect changing naming conventions.
- Overly restrictive naming conventions that are hard to maintain.

### Expected Output
**Description:** A list of object names that do not comply with the specified naming convention.

**Schema:**
- OBJECT_NAME

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*