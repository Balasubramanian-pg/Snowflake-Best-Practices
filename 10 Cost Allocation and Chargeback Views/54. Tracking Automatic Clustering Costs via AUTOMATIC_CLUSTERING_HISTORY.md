# Tracking Automatic Clustering Costs via AUTOMATIC_CLUSTERING_HISTORY

**Description:**
This topic explains how to track the costs associated with automatic clustering in Snowflake using the AUTOMATIC_CLUSTERING_HISTORY view. Automatic clustering is a feature that automatically re-clusters data in a table to improve query performance. The AUTOMATIC_CLUSTERING_HISTORY view provides a record of all automatic clustering operations performed on a table, including the costs associated with these operations.

## Short Explanation
**Overview:** The AUTOMATIC_CLUSTERING_HISTORY view provides a record of all automatic clustering operations performed on a table.

**Problem Solved:** This feature solves the problem of tracking the costs associated with automatic clustering operations, allowing users to monitor and optimize their clustering costs.

**Importance:** This is important in analytics and warehousing because it allows users to understand the costs associated with improving query performance through automatic clustering.

**Use Cases:**
- Monitoring clustering costs for a specific table
- Optimizing clustering operations to reduce costs

### Typical Scenario
A business scenario where a data analyst wants to track the costs associated with automatic clustering operations on a specific table. For example, they may want to monitor the costs of clustering operations on a large fact table in a data warehouse.

### Core Snowflake SQL Objects Used
`AUTOMATIC_CLUSTERING_HISTORY`

### Code Source Execution
```sql
SELECT * FROM AUTOMATIC_CLUSTERING_HISTORY;
```

### Example Execution Logic Step-by-Step
The AUTOMATIC_CLUSTERING_HISTORY view provides a record of all automatic clustering operations performed on a table. The view includes columns such as the operation start and end times, the number of bytes re-clustered, and the costs associated with the operation.

### Implementation Example
To track the costs associated with automatic clustering operations on a specific table, you can query the AUTOMATIC_CLUSTERING_HISTORY view and filter the results by table name. For example:

SELECT * FROM AUTOMATIC_CLUSTERING_HISTORY WHERE table_name = 'my_table';

### Explanation of the Code
- Step 1: Query the AUTOMATIC_CLUSTERING_HISTORY view to retrieve a record of all automatic clustering operations.
- Step 2: Filter the results by table name to focus on the specific table of interest.

### When to Use
- When you want to track the costs associated with automatic clustering operations on a specific table
- When you want to optimize clustering operations to reduce costs

### When Not to Use
- When you are not using automatic clustering on a table
- When you are not concerned with tracking clustering costs

### Common Mistakes
- Not filtering the results by table name, leading to a large result set
- Not understanding the costs associated with clustering operations

### Expected Output
**Description:** The result set includes columns such as the operation start and end times, the number of bytes re-clustered, and the costs associated with the operation.

**Schema:**
- operation_start_time
- operation_end_time
- table_name
- bytes_reclustered
- cost

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*