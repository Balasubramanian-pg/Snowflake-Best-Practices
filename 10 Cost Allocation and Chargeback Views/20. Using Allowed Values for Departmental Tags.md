# Using Allowed Values for Departmental Tags in Snowflake

**Description:**
This topic explains how to utilize allowed values for departmental tags in Snowflake, ensuring data consistency and governance. By defining allowed values for tags, organizations can standardize and streamline their data cataloging process. This feature enables better data discovery, cost allocation, and chargeback views.

## Short Explanation
**Overview:** Allowed values for departmental tags in Snowflake enable organizations to standardize their data cataloging process.

**Problem Solved:** Inconsistent data tagging leads to difficulties in data discovery and cost allocation.

**Importance:** Standardized tagging is crucial for efficient data governance and cost management in Snowflake.

**Use Cases:**
- Standardizing departmental tags for cost allocation
- Implementing data governance policies using allowed values

### Typical Scenario
A company wants to allocate costs to different departments based on standardized tags. They define allowed values for departmental tags, such as 'Sales', 'Marketing', and 'IT', to ensure consistency across their Snowflake instance.

### Core Snowflake SQL Objects Used
`CREATE TAG`, `ALTER TAG`

### Code Source Execution
```sql
CREATE TAG departmental_tag ALLOWED_VALUES ('Sales', 'Marketing', 'IT');
```

### Example Execution Logic Step-by-Step
To implement allowed values for departmental tags, first create a new tag with the ALLOWED_VALUES clause. For example, create a tag called 'departmental_tag' with allowed values 'Sales', 'Marketing', and 'IT'.

### Implementation Example
CREATE TAG departmental_tag ALLOWED_VALUES ('Sales', 'Marketing', 'IT');
ALTER TAG departmental_tag SET ALLOWED_VALUES ('Sales', 'Marketing', 'IT', 'HR');

### Explanation of the Code
- Step 1: Create a new tag with the ALLOWED_VALUES clause.
- Step 2: Alter the tag to add or modify allowed values as needed.

### When to Use
- When implementing data governance policies
- When standardizing data cataloging across departments

### When Not to Use
- When there are no clear departmental boundaries
- When tags are not required for data discovery

### Common Mistakes
- Not defining allowed values for tags
- Using inconsistent tagging across departments

### Expected Output
**Description:** The result set will contain the allowed values for the departmental tag, ensuring data consistency and governance.

**Schema:**
- tag_name
- allowed_values

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*