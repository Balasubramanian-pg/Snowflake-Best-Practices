# Understanding the Default Suspended State of New Tasks in Snowflake

**Description:**
This topic explains the default behavior of new tasks in Snowflake, specifically why they are created in a suspended state. It covers the implications of this behavior and best practices for managing tasks. By default, new tasks in Snowflake are created in a suspended state to prevent accidental execution and allow for proper configuration. Understanding this behavior is crucial for effective task management.

## Short Explanation
**Overview:** New tasks in Snowflake are created in a suspended state by default.

**Problem Solved:** Prevents accidental execution of tasks and allows for proper configuration before activation.

**Importance:** Ensures data integrity and prevents unintended resource utilization.

**Use Cases:**
- Data pipeline management
- Automated data processing

### Typical Scenario
A data engineer creates a new task to automate data loading from an external source. The task is initially created in a suspended state, allowing the engineer to review and configure the task before activating it.

### Core Snowflake SQL Objects Used
`CREATE TASK`

### Code Source Execution
```sql
CREATE TASK my_task
  WAREHOUSE = 'my_warehouse'
  SCHEDULE = '0 1 * * *'
  COMMENT = 'My task'
AS
  CALL my_stored_procedure();
```

### Example Execution Logic Step-by-Step
The task is created in a suspended state, requiring an ALTER TASK statement to set the state to 'started' or 'resume'. For example: ALTER TASK my_task SET STATE = 'started';

### Implementation Example
To implement a task, create it in a suspended state, configure the task properties, and then activate it. For example:
  CREATE TASK my_task
    WAREHOUSE = 'my_warehouse'
    SCHEDULE = '0 1 * * *'
    COMMENT = 'My task'
  AS
    CALL my_stored_procedure();
  ALTER TASK my_task SET STATE = 'started';

### Explanation of the Code
- Step 1: Create a new task in a suspended state using the CREATE TASK statement.
- Step 2: Configure the task properties, such as the warehouse and schedule.
- Step 3: Activate the task using an ALTER TASK statement.

### When to Use
- Data pipeline management
- Automated data processing

### When Not to Use
- Ad-hoc data queries
- Interactive data exploration

### Common Mistakes
- Forgetting to configure task properties before activation
- Not testing tasks before deploying them

### Expected Output
**Description:** The task state is initially 'suspended', and it requires an ALTER TASK statement to change the state to 'started' or 'resume'.

**Schema:**
- Task Name
- State
- Schedule
- Warehouse

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*