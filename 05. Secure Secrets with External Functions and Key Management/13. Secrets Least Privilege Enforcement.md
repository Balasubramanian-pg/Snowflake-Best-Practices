# Secrets Least Privilege Enforcement

**Description:**
This topic is not clearly defined in the provided file as it seems to be related to Dynamic Data Masking in Snowflake, not Secrets Least Privilege Enforcement. The file appears to be incorrectly named or the content is not related to the title.

## Short Explanation
**Overview:** Dynamic Data Masking is a feature that masks sensitive data at query time.

**Problem Solved:** Unauthorized access to sensitive data.

**Importance:** Protects sensitive data and meets compliance requirements.

**Use Cases:**
- Securing customer PII data\n- Protecting financial information

### Typical Scenario
A company wants to provide analysts with access to customer data, but the data contains sensitive information such as social security numbers and credit card numbers. The company uses DDM to mask this sensitive information.

### Core Snowflake SQL Objects Used
`CREATE MASKING POLICY`, `ALTER TABLE`

### Code Source Execution
```sql
CREATE MASKING POLICY mask_sensitive_data AS (val string) RETURNS string -> CASE WHEN CURRENT_ROLE() IN ('ANALYST') THEN val ELSE 'XXXX' END;
```

### Example Execution Logic Step-by-Step
To implement DDM, first create a masking policy using the CREATE MASKING POLICY statement. Then, apply the policy to a table or column using the ALTER TABLE statement.

### Implementation Example
CREATE TABLE customers (id int, name string, ssn string); CREATE MASKING POLICY mask_ssn AS (val string) RETURNS string -> CASE WHEN CURRENT_ROLE() IN ('ANALYST') THEN val ELSE 'XXXX' END; ALTER TABLE customers MODIFY COLUMN ssn SET MASKING POLICY mask_ssn;

### Explanation of the Code
- - Create a masking policy that masks the sensitive data with 'XXXX' for roles that do not have analyst privileges.\n- - Apply the masking policy to the ssn column in the customers table.

### When to Use
- When sensitive data needs to be protected from unauthorized access\n- When meeting compliance requirements for data protection

### When Not to Use
- When all users need access to sensitive data\n- When data is already encrypted

### Common Mistakes
- Not testing masking policies thoroughly\n- Not considering performance impact

### Expected Output
**Description:** The result set will contain masked sensitive data for users without analyst privileges.

**Schema:**
- id\n- name\n- ssn

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*