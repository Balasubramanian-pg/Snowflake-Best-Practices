# Integrating Google Cloud Pub/Sub with Snowflake

**Description:**
This topic describes how to integrate Google Cloud Pub/Sub with Snowflake, enabling real-time data ingestion and event-driven pipelines. By connecting Pub/Sub to Snowflake, you can stream data directly into Snowflake for analysis and processing. This integration allows for efficient handling of high-volume, high-velocity data streams.

## Short Explanation
**Overview:** Integrating Google Cloud Pub/Sub with Snowflake enables real-time data ingestion from Pub/Sub topics into Snowflake.

**Problem Solved:** Handling high-volume, high-velocity data streams from Pub/Sub in Snowflake.

**Importance:** Enables real-time analytics and event-driven pipelines in Snowflake.

**Use Cases:**
- Streaming IoT sensor data into Snowflake
- Ingesting log data from applications into Snowflake

### Typical Scenario
A company uses Google Cloud Pub/Sub to collect log data from its applications. The log data is then streamed into Snowflake using the Pub/Sub integration, allowing for real-time analysis and monitoring of application performance.

### Core Snowflake SQL Objects Used
`CREATE STORAGE INTEGRATION`, `CREATE PIPE`

### Code Source Execution
```sql
CREATE STORAGE INTEGRATION gcp_pubsub_integration TYPE = 'STORAGE' STORAGE_PROVIDER = 'GCP' STORAGE_AWS_ROLE_ARN = 'arn:aws:iam::...';
```

### Example Execution Logic Step-by-Step
To integrate Pub/Sub with Snowflake, you need to create a storage integration and a pipe. The storage integration defines the connection to Pub/Sub, while the pipe specifies the topic and subscription to use.

### Implementation Example
CREATE STORAGE INTEGRATION gcp_pubsub_integration TYPE = 'STORAGE' STORAGE_PROVIDER = 'GCP' STORAGE_AWS_ROLE_ARN = 'arn:aws:iam::...';
CREATE PIPE my_pipe FOR COPY INTO my_table FROM (SELECT SUBSTRING(SPLIT(data: message, '::'), 2)) AT OFFSET 0 STORAGE_INTEGRATION gcp_pubsub_integration PUBSUB_SUBSCRIPTION = 'my_subscription' PUBSUB_TOPIC = 'my_topic';

### Explanation of the Code
- Create a storage integration with GCP as the storage provider.
- Create a pipe that copies data from Pub/Sub into a Snowflake table.

### When to Use
- Real-time data ingestion from Pub/Sub
- Event-driven pipelines

### When Not to Use
- Batch data loading
- Small-scale data ingestion

### Common Mistakes
- Incorrect storage integration configuration
- Insufficient permissions for Pub/Sub subscription

### Expected Output
**Description:** The resulting table in Snowflake contains the ingested data from Pub/Sub.

**Schema:**
- Column 1
- Column 2

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*