# Dynamic Data Masking in Snowflake

**Description:**
Dynamic Data Masking (DDM) is a security feature in Snowflake that allows administrators to limit sensitive data exposure by masking it to unauthorized users. DDM ensures that sensitive data is not accidentally exposed to users who do not have the necessary privileges to view it. This feature helps organizations to meet compliance requirements and protect sensitive data. DDM can be applied to specific columns in a table.

## Short Explanation
**Overview:** Dynamic Data Masking is a feature that masks sensitive data at query time.

**Problem Solved:** Unauthorized access to sensitive data.

**Importance:** Protects sensitive data and meets compliance requirements.

**Use Cases:**
- Securing customer PII data\n- Protecting financial data

### Typical Scenario
A company wants to expose customer data to analysts but needs to ensure that sensitive information such as credit card numbers and social security numbers are not visible.

### Core Snowflake SQL Objects Used
`CREATE MASKING POLICY`, `ALTER TABLE`

### Code Source Execution
```sql
CREATE MASKING POLICY mask_cc_number AS (val string) RETURNS string -> 'XXXX-XXXX-XXXX-' || SUBSTR(val, -4);
```

### Example Execution Logic Step-by-Step
To implement DDM, first create a masking policy using the CREATE MASKING POLICY statement. Then apply the policy to a column in a table using the ALTER TABLE statement.

### Implementation Example
CREATE TABLE customers (id INT, name STRING, cc_number STRING); CREATE MASKING POLICY mask_cc_number AS (val string) RETURNS string -> 'XXXX-XXXX-XXXX-' || SUBSTR(val, -4); ALTER TABLE customers MODIFY COLUMN cc_number SET MASKING POLICY mask_cc_number;

### Explanation of the Code
- Create a table with a sensitive column (cc_number).\n- Create a masking policy that masks all but the last 4 characters of the credit card number.\n- Apply the masking policy to the cc_number column.

### When to Use
- When sensitive data needs to be protected from unauthorized access.\n- When compliance regulations require data masking.

### When Not to Use
- When performance is critical and masking overhead is a concern.\n- When data is already encrypted and access is restricted.

### Common Mistakes
- Forgetting to apply the masking policy to the correct columns.\n- Not testing the masking policy thoroughly.

### Expected Output
**Description:** The masked credit card number.

**Schema:**
- id\n- name\n- cc_number

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*