# Filtering QUERY_HISTORY by Session, User, and Warehouse in Snowflake

**Description:**
This topic explains how to filter the QUERY_HISTORY table in Snowflake by session, user, and warehouse. It provides insights into optimizing query analysis and monitoring. By filtering QUERY_HISTORY, users can efficiently track and manage query performance.

## Short Explanation
**Overview:** QUERY_HISTORY is a system table in Snowflake that stores a history of all queries executed in an account.

**Problem Solved:** Filtering QUERY_HISTORY helps to quickly identify and analyze specific queries based on session, user, or warehouse, making it easier to optimize performance and troubleshoot issues.

**Importance:** This feature is crucial for analytics and data warehousing as it enables users to monitor query performance, optimize resource utilization, and ensure efficient data processing.

**Use Cases:**
- Analyzing query performance for a specific user
- Monitoring queries executed on a particular warehouse
- Tracking queries for a specific session

### Typical Scenario
A data analyst wants to analyze the performance of queries executed by a specific user on a particular warehouse. They can use the QUERY_HISTORY table and filter it by the user and warehouse to get the required information.

### Core Snowflake SQL Objects Used


### Code Source Execution
```sql
SELECT * FROM TABLE(INFORMATION_SCHEMA.QUERY_HISTORY_BY_SESSION()) WHERE USER_NAME = ' Analyst1' AND WAREHOUSE_NAME = 'WH1';
```

### Example Execution Logic Step-by-Step
The QUERY_HISTORY_BY_SESSION() function returns a table with query history for the current session. By filtering this table using the USER_NAME and WAREHOUSE_NAME columns, users can get specific query history.

### Implementation Example
To filter QUERY_HISTORY by session, user, and warehouse, use the following SQL queries:

-- Filter by session
SELECT * FROM TABLE(INFORMATION_SCHEMA.QUERY_HISTORY_BY_SESSION());

-- Filter by user
SELECT * FROM TABLE(INFORMATION_SCHEMA.QUERY_HISTORY()) WHERE USER_NAME = ' Analyst1';

-- Filter by warehouse
SELECT * FROM TABLE(INFORMATION_SCHEMA.QUERY_HISTORY()) WHERE WAREHOUSE_NAME = 'WH1';

### Explanation of the Code
- The QUERY_HISTORY_BY_SESSION() function returns query history for the current session.
- The QUERY_HISTORY() function returns query history for the entire account.
- Filtering QUERY_HISTORY by USER_NAME and WAREHOUSE_NAME columns allows users to get specific query history.

### When to Use
- Analyzing query performance for a specific user or warehouse
- Monitoring queries for a specific session

### When Not to Use
- When analyzing query performance for all users or warehouses
- When monitoring queries for a short period

### Common Mistakes
- Not specifying the correct session, user, or warehouse when filtering QUERY_HISTORY
- Not using the correct functions (QUERY_HISTORY_BY_SESSION() vs QUERY_HISTORY())

### Expected Output
**Description:** The result set will contain columns such as QUERY_ID, USER_NAME, WAREHOUSE_NAME, QUERY_TEXT, START_TIME, and END_TIME.

**Schema:**
- QUERY_ID
- USER_NAME
- WAREHOUSE_NAME
- QUERY_TEXT
- START_TIME
- END_TIME

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*