# Adding a Clustering Key to an Existing Table in Snowflake

**Description:**
This topic explains how to add a clustering key to an existing table in Snowflake, which helps improve query performance by optimizing data storage and retrieval.

## Short Explanation
**Overview:** Adding a clustering key to an existing table in Snowflake allows for efficient data organization and retrieval.

**Problem Solved:** Improves query performance by reducing the amount of data that needs to be scanned.

**Importance:** Enhances analytics and warehousing capabilities by speeding up data access.

**Use Cases:**
- Optimizing large table queries
- Improving data warehouse performance

### Typical Scenario
A business with a large e-commerce database wants to speed up queries on customer order history.

### Core Snowflake SQL Objects Used
`ALTER TABLE`

### Code Source Execution
```sql
ALTER TABLE existing_table CLUSTER BY (column1, column2);
```

### Example Execution Logic Step-by-Step
To add a clustering key to an existing table, use the ALTER TABLE command with the CLUSTER BY clause, specifying the columns to be used for clustering.

### Implementation Example
ALTER TABLE orders CLUSTER BY (customer_id, order_date);

### Explanation of the Code
- Step 1: Identify the columns to be used for clustering (e.g., customer_id, order_date).
- Step 2: Use the ALTER TABLE command with the CLUSTER BY clause to add the clustering key.

### When to Use
- When queries frequently filter on specific columns
- When tables are very large

### When Not to Use
- When tables are small
- When queries do not filter on specific columns

### Common Mistakes
- Choosing columns with low cardinality
- Not monitoring query performance after adding clustering key

### Expected Output
**Description:** The table will be re-clustered according to the new clustering key, and future queries will benefit from improved performance.

**Schema:**
- Column 1
- Column 2

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*