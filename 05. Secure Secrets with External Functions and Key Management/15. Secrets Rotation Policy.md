# Dynamic Data Masking in Snowflake

**Description:**
Dynamic Data Masking (DDM) is a security feature in Snowflake that allows administrators to limit sensitive data exposure by masking it to unauthorized users. DDM applies masks to data at query time, ensuring that users only see authorized data. This feature helps protect sensitive data while maintaining data usability.

## Short Explanation
**Overview:** Dynamic Data Masking (DDM) is a Snowflake feature that masks sensitive data at query time.

**Problem Solved:** DDM solves the problem of unauthorized data access by masking sensitive information.

**Importance:** DDM is crucial in analytics and warehousing for protecting sensitive data while maintaining data usability.

**Use Cases:**
- Securing customer PII data\n- Protecting financial information

### Typical Scenario
A company wants to expose customer data to business analysts while protecting sensitive information like social security numbers and credit card numbers.

### Core Snowflake SQL Objects Used
`CREATE MASKING POLICY`, `ALTER TABLE`

### Code Source Execution
```sql
CREATE MASKING POLICY mask_sensitive_data AS (val string) RETURNS string -> CASE WHEN CURRENT_ROLE() = 'ANALYST' THEN val ELSE 'XXXX' END;
```

### Example Execution Logic Step-by-Step
To implement DDM, first create a masking policy using CREATE MASKING POLICY. Then, apply the policy to a table column using ALTER TABLE. When a user queries the table, Snowflake applies the mask at query time.

### Implementation Example
CREATE TABLE customers (id INT, name STRING, ssn STRING); CREATE MASKING POLICY mask_ssn AS (val string) RETURNS string -> CASE WHEN CURRENT_ROLE() = 'ANALYST' THEN val ELSE 'XXXX' END; ALTER TABLE customers MODIFY COLUMN ssn SET MASKING POLICY mask_ssn;

### Explanation of the Code
- Create a table with sensitive data.\n- Create a masking policy that masks data based on the current role.\n- Apply the masking policy to the sensitive column.

### When to Use
- Protecting PII data\n- Securing financial data

### When Not to Use
- For non-sensitive data\n- When data encryption is sufficient

### Common Mistakes
- Not testing masking policies thoroughly\n- Overly permissive access controls

### Expected Output
**Description:** The result set will show masked data for unauthorized users.

**Schema:**
- id\n- name\n- ssn

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*