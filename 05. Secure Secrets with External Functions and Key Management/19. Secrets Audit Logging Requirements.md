# Dynamic Data Masking in Snowflake

**Description:**
Dynamic Data Masking (DDM) is a security feature in Snowflake that allows administrators to limit sensitive data exposure by masking it to unauthorized users. DDM ensures that sensitive data is not accidentally exposed to users who do not have the necessary privileges to view it. This feature helps organizations to meet compliance requirements and protect sensitive data.

## Short Explanation
**Overview:** Dynamic Data Masking is a feature that masks sensitive data at query time.

**Problem Solved:** Unauthorized access to sensitive data.

**Importance:** Protects sensitive data and meets compliance requirements.

**Use Cases:**
- Securing customer PII data\n- Protecting financial information

### Typical Scenario
A company wants to provide analysts with access to customer data, but the data contains sensitive information such as social security numbers and credit card numbers. The company uses DDM to mask this sensitive information.

### Core Snowflake SQL Objects Used
`CREATE MASKING POLICY`, `ALTER TABLE`

### Code Source Execution
```sql
CREATE MASKING POLICY mask_sensitive_data AS (val string) RETURNS string -> CASE WHEN CURRENT_ROLE() IN ('ADMIN_ROLE') THEN val ELSE 'XXXX' END;
```

### Example Execution Logic Step-by-Step
To implement DDM, first create a masking policy using the CREATE MASKING POLICY statement. Then, apply the policy to a table column using the ALTER TABLE statement.

### Implementation Example
CREATE TABLE customers (id int, name string, ssn string); CREATE MASKING POLICY mask_ssn AS (val string) RETURNS string -> CASE WHEN CURRENT_ROLE() IN ('ADMIN_ROLE') THEN val ELSE 'XXXX' END; ALTER TABLE customers MODIFY COLUMN ssn SET MASKING POLICY mask_ssn;

### Explanation of the Code
- Create a table with a sensitive column (ssn).\n- Create a masking policy that masks the ssn column for non-admin roles.\n- Apply the masking policy to the ssn column.

### When to Use
- When sensitive data needs to be protected from unauthorized access\n- When meeting compliance requirements for data protection

### When Not to Use
- When performance is critical and masking overhead is a concern\n- When data is already encrypted and access is restricted

### Common Mistakes
- Forgetting to grant the necessary privileges to the masking policy\n- Not testing the masking policy thoroughly

### Expected Output
**Description:** The result set will contain masked data for unauthorized users.

**Schema:**
- id\n- name\n- ssn

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*