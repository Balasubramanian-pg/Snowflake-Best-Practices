# Naming Conventions for Snowflake Native Apps

**Description:**
This document outlines the best practices for naming Snowflake Native Apps, including guidelines for app names, stages, and other related objects. Following these conventions ensures consistency and readability across Snowflake Native App development. Well-structured names facilitate easier maintenance, collaboration, and understanding of the app's functionality.

## Short Explanation
**Overview:** Snowflake Native Apps require well-structured naming conventions for better organization and understanding.

**Problem Solved:** Inconsistent naming can lead to confusion and difficulties in managing Snowflake Native Apps.

**Importance:** Proper naming conventions are crucial for efficient collaboration, maintenance, and scalability of Snowflake Native Apps.

**Use Cases:**
- Developing a new Snowflake Native App
- Refactoring existing Snowflake Native App code

### Typical Scenario
A developer is creating a new Snowflake Native App called 'data_loader' and needs to create stages, pipes, and tasks with clear and descriptive names.

### Core Snowflake SQL Objects Used
`CREATE STAGE`, `CREATE PIPE`

### Code Source Execution
```sql
CREATE STAGE data_loader_stage
  URL = 's3://my-bucket/data-loader/'
  CREDENTIALS = (AWS_KEY_ID = '...' AWS_SECRET_KEY = '...');
```

### Example Execution Logic Step-by-Step
In this example, the stage name 'data_loader_stage' clearly indicates its purpose and relation to the 'data_loader' app. The URL and credentials specify the location and access details for the stage.

### Implementation Example
CREATE STAGE data_loader_stage
  URL = 's3://my-bucket/data-loader/'
  CREDENTIALS = (AWS_KEY_ID = '...' AWS_SECRET_KEY = '...');

CREATE PIPE data_loader_pipe
  FOR COPY INTO my_table
  FROM STAGE data_loader_stage
  AUTO_INGEST = TRUE;

### Explanation of the Code
- Step 1: Create a stage named 'data_loader_stage' with a clear URL and credentials.
- Step 2: Create a pipe named 'data_loader_pipe' that copies data from the stage into a table.

### When to Use
- When developing a new Snowflake Native App
- When refactoring existing Snowflake Native App code

### When Not to Use
- When creating a non-native Snowflake app
- When using an older Snowflake feature

### Common Mistakes
- Using unclear or ambiguous names for stages and pipes
- Not following consistent naming conventions across the app

### Expected Output
**Description:** The output will be a list of created stages and pipes with their respective properties.

**Schema:**
- name
- url
- credentials

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*