# Handling Legacy Database Names During Snowflake Cloud Migrations

**Description:**
This topic provides guidance on handling legacy database names during Snowflake cloud migrations. It covers the challenges of migrating databases with non-standard names and provides best practices for renaming databases in Snowflake.

## Short Explanation
**Overview:** Migrating legacy databases to Snowflake requires careful handling of non-standard database names.

**Problem Solved:** Legacy database names may not conform to Snowflake's naming conventions, causing migration issues.

**Importance:** Proper handling of legacy database names ensures a smooth migration to Snowflake and avoids potential errors.

**Use Cases:**
- Migrating on-premises databases to Snowflake
- Renaming databases to conform to Snowflake naming conventions

### Typical Scenario
A company is migrating its on-premises databases to Snowflake and needs to handle legacy database names that do not conform to Snowflake's naming conventions.

### Core Snowflake SQL Objects Used
`ALTER DATABASE`, `CREATE DATABASE`

### Code Source Execution
```sql
ALTER DATABASE my_legacy_db RENAME TO my_new_db;
```

### Example Execution Logic Step-by-Step
To handle legacy database names, you can use the ALTER DATABASE RENAME statement to rename the database to a new name that conforms to Snowflake's naming conventions.

### Implementation Example
CREATE DATABASE my_new_db; ALTER DATABASE my_legacy_db RENAME TO my_new_db;

### Explanation of the Code
- Step 1: Create a new database with a name that conforms to Snowflake's naming conventions.
- Step 2: Use the ALTER DATABASE RENAME statement to rename the legacy database to the new database name.

### When to Use
- Migrating legacy databases to Snowflake
- Renaming databases to conform to Snowflake naming conventions

### When Not to Use
- When the legacy database name is already in use by another Snowflake database

### Common Mistakes
- Forgetting to update dependent objects, such as views and stored procedures, to use the new database name

### Expected Output
**Description:** The database is successfully renamed, and dependent objects are updated to use the new database name.

**Schema:**
- Database Name
- New Database Name

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*