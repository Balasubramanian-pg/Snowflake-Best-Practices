# SYSTEM$CLUSTERING_INFORMATION Function in Snowflake

**Description:**
The SYSTEM$CLUSTERING_INFORMATION function in Snowflake provides information about the clustering of a table, including the clustering depth, bucket overlap, and bucket count. This function helps to evaluate the effectiveness of a table's clustering and identify potential optimization opportunities. Clustering information is crucial for efficient query performance and data warehousing best practices.

## Short Explanation
**Overview:** The SYSTEM$CLUSTERING_INFORMATION function returns clustering metrics for a specified table.

**Problem Solved:** It helps to identify inefficient clustering, which can lead to slow query performance.

**Importance:** Proper clustering is essential for high-performance data warehousing and analytics.

**Use Cases:**
- Evaluating clustering effectiveness
- Optimizing table clustering for better query performance

### Typical Scenario
A data analyst wants to evaluate the clustering effectiveness of a large fact table in a Snowflake data warehouse. They use the SYSTEM$CLUSTERING_INFORMATION function to get clustering metrics and identify potential optimization opportunities.

### Core Snowflake SQL Objects Used


### Code Source Execution
```sql
SELECT SYSTEM$CLUSTERING_INFORMATION('my_table', 'my_cluster_key') AS clustering_info;
```

### Example Execution Logic Step-by-Step
The SYSTEM$CLUSTERING_INFORMATION function takes two arguments: the table name and the clustering key. It returns a JSON object containing clustering metrics, such as clustering depth, bucket overlap, and bucket count.

### Implementation Example
To evaluate the clustering of a table named 'orders' with a clustering key 'order_date', use the following query: SELECT SYSTEM$CLUSTERING_INFORMATION('orders', 'order_date') AS clustering_info;

### Explanation of the Code
- The SYSTEM$CLUSTERING_INFORMATION function is a system function in Snowflake that returns clustering metrics.
- The function takes two arguments: the table name and the clustering key.
- The returned JSON object contains clustering metrics, such as clustering depth, bucket overlap, and bucket count.

### When to Use
- Evaluating clustering effectiveness
- Optimizing table clustering for better query performance

### When Not to Use
- On small tables with minimal data
- On tables with poor data distribution

### Common Mistakes
- Not specifying the correct clustering key
- Not evaluating clustering metrics regularly

### Expected Output
**Description:** The function returns a JSON object containing clustering metrics.

**Schema:**
- CLUSTERING_DEPTH
- BUCKET_OVERLAP
- BUCKET_COUNT

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*