# Analyzing Daily Currency Usage Across Accounts

**Description:**
This topic focuses on analyzing the USAGE_IN_CURRENCY_DAILY view across multiple accounts in Snowflake, enabling organizations to track and manage their daily currency usage and costs effectively.

## Short Explanation
**Overview:** The USAGE_IN_CURRENCY_DAILY view provides daily usage data in the customer's currency, allowing for effective cost analysis and management across accounts.

**Problem Solved:** It solves the problem of tracking and analyzing daily currency usage across multiple accounts, making it easier to manage costs and optimize resource utilization.

**Importance:** This matters in analytics and warehousing as it enables organizations to make data-driven decisions about resource allocation and cost optimization.

**Use Cases:**
- Analyzing daily costs across different departments or business units
- Tracking currency usage trends over time to optimize resource utilization

### Typical Scenario
A company with multiple Snowflake accounts across different regions wants to analyze their daily currency usage to optimize costs and resource allocation.

### Core Snowflake SQL Objects Used
`CREATE VIEW daily_currency_usage AS SELECT * FROM USAGE_IN_CURRENCY_DAILY`, `CREATE MATERIALIZED VIEW daily_currency_usage_mv AS SELECT account, currency, date, SUM(usage) AS total_usage FROM USAGE_IN_CURRENCY_DAILY GROUP BY account, currency, date`

### Code Source Execution
```sql
CREATE VIEW daily_currency_usage AS SELECT account, currency, date, usage FROM USAGE_IN_CURRENCY_DAILY;
```

### Example Execution Logic Step-by-Step
This SQL example creates a view that directly accesses the USAGE_IN_CURRENCY_DAILY view, providing a simplified way to analyze daily currency usage across accounts.

### Implementation Example
To implement a materialized view for faster query performance, you can use the following SQL: CREATE MATERIALIZED VIEW daily_currency_usage_mv AUTO_REFRESH = TRUE AS SELECT account, currency, date, SUM(usage) AS total_usage FROM USAGE_IN_CURRENCY_DAILY GROUP BY account, currency, date;

### Explanation of the Code
- The SQL creates a view or materialized view over the USAGE_IN_CURRENCY_DAILY system view.
- It selects key columns such as account, currency, date, and usage (or total_usage for aggregated data).

### When to Use
- When you need to analyze daily currency usage across multiple accounts.
- When you want to track and manage costs effectively across your organization.

### When Not to Use
- When real-time data is not critical, and historical data analysis suffices.
- When detailed per-second usage data is required; consider other views like USAGE_IN_CURRENCY.

### Common Mistakes
- Not properly aggregating data, leading to incorrect insights.
- Overlooking the need for access control and data governance when analyzing sensitive cost data.

### Expected Output
**Description:** The expected output includes columns for account, currency, date, and usage (or total_usage), providing a clear overview of daily currency usage across accounts.

**Schema:**
- account
- currency
- date
- usage

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*