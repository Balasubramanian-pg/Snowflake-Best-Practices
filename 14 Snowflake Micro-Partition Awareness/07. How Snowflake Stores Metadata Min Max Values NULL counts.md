# Metadata Storage in Snowflake: Min, Max, and NULL Value Counts

**Description:**
Snowflake stores metadata about each micro-partition, including minimum and maximum values, as well as NULL value counts. This metadata enables efficient pruning and filtering of data during query execution. By storing this information, Snowflake can optimize query performance and reduce the amount of data that needs to be scanned. This feature is particularly useful for large datasets and complex queries.

## Short Explanation
**Overview:** Snowflake stores metadata about each micro-partition to optimize query performance.

**Problem Solved:** Reduces the amount of data that needs to be scanned during query execution.

**Importance:** Improves query performance and reduces compute costs.

**Use Cases:**
- Query optimization
- Data pruning
- Micro-partitioning

### Typical Scenario
A large e-commerce company uses Snowflake to store sales data. By storing metadata about each micro-partition, Snowflake can quickly prune partitions that don't contain relevant data, reducing the amount of data that needs to be scanned during query execution.

### Core Snowflake SQL Objects Used


### Code Source Execution
```sql
-- no snippet mapped
```

### Example Execution Logic Step-by-Step
Snowflake automatically generates and stores metadata about each micro-partition. This metadata includes:
    - Minimum and maximum values for each column
    - NULL value counts for each column
    When a query is executed, Snowflake uses this metadata to prune partitions that don't contain relevant data.

### Implementation Example


### Explanation of the Code


### When to Use
- Large datasets
- Complex queries
- Query optimization

### When Not to Use
- Small datasets
- Simple queries

### Common Mistakes
- Not leveraging metadata for query optimization
- Not pruning partitions effectively

### Expected Output
**Description:** The metadata stored by Snowflake, including minimum and maximum values, as well as NULL value counts.

**Schema:**
- Column Name
- Min Value
- Max Value
- NULL Count

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*