# Integrating Task Notifications with Email Alerts in Snowflake

**Description:**
This topic explains how to set up email alerts for task notifications in Snowflake, enabling users to receive notifications when tasks complete or fail. This integration enhances monitoring and automation capabilities in Snowflake. By leveraging email alerts, users can stay informed about task execution results without needing to constantly monitor the Snowflake interface.

## Short Explanation
**Overview:** Integrating task notifications with email alerts allows users to receive task execution results via email.

**Problem Solved:** Users can stay informed about task execution results without constant monitoring.

**Importance:** Enhances monitoring and automation in Snowflake.

**Use Cases:**
- Automated data pipeline monitoring
- Task failure notification

### Typical Scenario
A data engineer sets up a daily data pipeline using Snowflake tasks. To ensure the pipeline's success, they integrate email alerts for task notifications. When a task fails or completes, an email notification is sent to the engineering team, allowing them to respond promptly to any issues.

### Core Snowflake SQL Objects Used
`CREATE TASK`, `ALTER TASK`

### Code Source Execution
```sql
CREATE TASK my_task
  WAREHOUSE = 'my_warehouse'
  SCHEDULE = '0 8 * * *'
  COMMENT = 'Daily data pipeline task'
  CALL my_procedure();

ALTER TASK my_task
  SET EMAIL NOTIFICATIONS = (ON_FAILURE = 'recipient@example.com');
```

### Example Execution Logic Step-by-Step
To integrate task notifications with email alerts, create a task with a schedule and associate it with a stored procedure. Then, alter the task to set email notifications for failure.

### Implementation Example
CREATE PROCEDURE my_procedure()
  LANGUAGE SQL
  EXECUTE AS CALLER
  BEGIN
    -- Procedure implementation
  END;

CREATE TASK my_task
  WAREHOUSE = 'my_warehouse'
  SCHEDULE = '0 8 * * *'
  COMMENT = 'Daily data pipeline task'
  CALL my_procedure();

ALTER TASK my_task
  SET EMAIL NOTIFICATIONS = (ON_FAILURE = 'recipient@example.com', ON_SUCCESS = 'recipient@example.com');

### Explanation of the Code
- Step 1: Create a stored procedure that performs the desired task.
- Step 2: Create a task with a schedule and associate it with the stored procedure.
- Step 3: Alter the task to set email notifications for failure and/or success.

### When to Use
- Automated data pipelines
- Scheduled task monitoring

### When Not to Use
- Ad-hoc queries
- Interactive data exploration

### Common Mistakes
- Not specifying email recipients correctly
- Not testing email notifications

### Expected Output
**Description:** Email notifications with task execution results.

**Schema:**
- Subject
- Body

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*