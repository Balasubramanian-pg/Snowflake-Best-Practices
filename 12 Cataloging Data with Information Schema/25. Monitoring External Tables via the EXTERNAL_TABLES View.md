# Monitoring External Tables via the EXTERNAL_TABLES View

**Description:**
This topic explains how to monitor external tables in Snowflake using the EXTERNAL_TABLES view. The EXTERNAL_TABLES view provides metadata about external tables, allowing users to track their status, refresh history, and other relevant information. By leveraging this view, users can effectively manage and troubleshoot their external tables.

## Short Explanation
**Overview:** The EXTERNAL_TABLES view provides metadata about external tables in Snowflake.

**Problem Solved:** Users can monitor and manage external tables, tracking their status and refresh history.

**Importance:** Effective monitoring and management of external tables is crucial for data consistency and reliability.

**Use Cases:**
- Monitoring external table refresh history
- Troubleshooting external table issues

### Typical Scenario
A data engineer wants to monitor the refresh history of an external table used for daily data loading. They query the EXTERNAL_TABLES view to track the table's status and refresh history.

### Core Snowflake SQL Objects Used
`CREATE VIEW`

### Code Source Execution
```sql
SELECT * FROM information_schema.external_tables;
```

### Example Execution Logic Step-by-Step
The EXTERNAL_TABLES view contains metadata about external tables, including table name, schema name, refresh history, and status. Users can query this view to retrieve information about their external tables.

### Implementation Example
To monitor the refresh history of an external table, execute the following query:
```
SELECT table_name, schema_name, last_refresh_time, refresh_status
FROM information_schema.external_tables;
```

### Explanation of the Code
- Step 1: The query selects the table name, schema name, last refresh time, and refresh status from the EXTERNAL_TABLES view.
- Step 2: The results provide the user with the necessary information to track the status and refresh history of their external tables.

### When to Use
- Monitoring external table refresh history
- Troubleshooting external table issues

### When Not to Use
- Retrieving data from external tables (use the external table itself instead)

### Common Mistakes
- Confusing the EXTERNAL_TABLES view with other information schema views

### Expected Output
**Description:** The result set contains metadata about external tables, including table name, schema name, refresh history, and status.

**Schema:**
- table_name
- schema_name
- last_refresh_time
- refresh_status

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*