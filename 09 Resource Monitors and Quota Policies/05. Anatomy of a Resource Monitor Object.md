# Anatomy of a Resource Monitor Object in Snowflake

**Description:**
This topic explains the structure and components of a Resource Monitor object in Snowflake, which is used to monitor and manage resource usage in a Snowflake account.

## Short Explanation
**Overview:** A Resource Monitor object in Snowflake is used to track and manage resource usage, such as credits, warehouse usage, and query performance.

**Problem Solved:** It helps organizations monitor and control their Snowflake resource usage, preventing unexpected costs and optimizing resource allocation.

**Importance:** Resource Monitors are crucial in Snowflake for ensuring efficient resource utilization, cost management, and performance optimization.

**Use Cases:**
- Monitoring warehouse usage and credits
- Tracking query performance and resource utilization

### Typical Scenario
A business scenario where a company wants to monitor and manage its Snowflake resource usage to optimize costs and performance. For example, a company can create a Resource Monitor to track the usage of its warehouses and alert administrators when usage exceeds a certain threshold.

### Core Snowflake SQL Objects Used
`CREATE RESOURCE MONITOR`

### Code Source Execution
```sql
CREATE RESOURCE MONITOR my_resource_monitor
  WITH (WAREHOUSE = 'my_warehouse',
        CREDIT_QUOTA = 100,
        NOTIFY_USERS = 'admin@example.com')
  USING (WAREHOUSE_LOAD = 100);
```

### Example Execution Logic Step-by-Step
This example creates a Resource Monitor named 'my_resource_monitor' that tracks the usage of the 'my_warehouse' warehouse and alerts the 'admin@example.com' user when the credit quota exceeds 100. The Resource Monitor uses the WAREHOUSE_LOAD metric to track warehouse usage.

### Implementation Example
To implement a Resource Monitor, you can use the following SQL commands:
  CREATE RESOURCE MONITOR my_resource_monitor
    WITH (WAREHOUSE = 'my_warehouse',
          CREDIT_QUOTA = 100,
          NOTIFY_USERS = 'admin@example.com')
    USING (WAREHOUSE_LOAD = 100);

  ALTER RESOURCE MONITOR my_resource_monitor SET CREDIT_QUOTA = 50;

### Explanation of the Code
- Step 1: Create a Resource Monitor object using the CREATE RESOURCE MONITOR command.
- Step 2: Specify the warehouse to monitor, credit quota, and notification users.
- Step 3: Define the metric to track (e.g., WAREHOUSE_LOAD).
- Step 4: Alter the Resource Monitor to adjust settings as needed.

### When to Use
- When you want to monitor and manage Snowflake resource usage
- When you need to optimize resource allocation and costs

### When Not to Use
- When you don't need to track resource usage
- When you have a small Snowflake deployment with minimal resource usage

### Common Mistakes
- Not setting up notifications for Resource Monitors
- Not regularly reviewing Resource Monitor metrics and adjusting settings

### Expected Output
**Description:** The output of a Resource Monitor includes metrics such as warehouse usage, credit quota, and notification messages.

**Schema:**
- WAREHOUSE_NAME
- CREDIT_QUOTA
- WAREHOUSE_LOAD
- NOTIFY_USERS

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*