# Appending Timestamps for Point-in-Time Clones in Snowflake

**Description:**
This Snowflake best practice involves appending timestamps to cloned databases or schemas to create point-in-time copies. This allows for the creation of historical versions of data that can be used for auditing, compliance, or data recovery purposes.

## Short Explanation
**Overview:** Appending timestamps to cloned databases or schemas in Snowflake enables the creation of point-in-time copies of data.

**Problem Solved:** This approach solves the problem of maintaining historical versions of data for auditing, compliance, or data recovery purposes.

**Importance:** It matters in analytics and warehousing as it provides a way to track changes to data over time and ensure data integrity.

**Use Cases:**
- Auditing and compliance
- Data recovery
- Data versioning

### Typical Scenario
A company wants to create a historical copy of its database or schema at a specific point in time for auditing purposes. They use Snowflake's cloning feature and append a timestamp to the cloned database or schema name.

### Core Snowflake SQL Objects Used
`CREATE DATABASE`, `CREATE SCHEMA`

### Code Source Execution
```sql
CREATE DATABASE mydatabase_clone_2022_01_01;
```

### Example Execution Logic Step-by-Step
To create a point-in-time clone of a database or schema, you can use Snowflake's cloning feature and append a timestamp to the cloned database or schema name. For example, to clone a database named 'mydatabase' at a specific point in time, you can use the following SQL command: CREATE DATABASE mydatabase_clone_2022_01_01; This creates a new database named 'mydatabase_clone_2022_01_01' that is a copy of 'mydatabase' at the specified point in time.

### Implementation Example
To implement this best practice, you can use a SQL script that takes the source database or schema name and the desired timestamp as input, and then creates a cloned database or schema with the appended timestamp. For example: CREATE OR REPLACE PROCEDURE create_point_in_time_clone(
    source_database_name string,
    timestamp string
)
RETURNS string
LANGUAGE javascript
EXECUTE AS CALLER
BEGIN
    var sql = `CREATE DATABASE ${source_database_name}_clone_${timestamp};`;
    snowflake.execute({sql: sql});
    return `Database clone created successfully`;
END;

### Explanation of the Code
- Step 1: Create a stored procedure that takes the source database or schema name and the desired timestamp as input.
- Step 2: Use the input parameters to construct a SQL command that creates a cloned database or schema with the appended timestamp.
- Step 3: Execute the SQL command using the Snowflake JavaScript API.

### When to Use
- When you need to create a historical copy of your data for auditing or compliance purposes
- When you need to create a point-in-time copy of your data for data recovery purposes

### When Not to Use
- When you need to create a real-time copy of your data
- When you need to create a copy of your data for testing or development purposes

### Common Mistakes
- Not appending a timestamp to the cloned database or schema name
- Not using a stored procedure to automate the cloning process

### Expected Output
**Description:** The output will be a new database or schema with the appended timestamp that is a copy of the source database or schema at the specified point in time.

**Schema:**
- Database name
- Schema name
- Timestamp

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*