# Combining Information Schema with ACCOUNT_USAGE in Snowflake

**Description:**
This topic explains how to leverage Snowflake's Information Schema and ACCOUNT_USAGE views to gain insights into account and usage metrics. By combining these two powerful features, users can create a comprehensive monitoring and analytics system. This integration enables data administrators to track usage patterns, optimize resources, and ensure cost-effective management of their Snowflake account.

## Short Explanation
**Overview:** Integrating Information Schema with ACCOUNT_USAGE provides a holistic view of account usage and schema metadata.

**Problem Solved:** This integration solves the problem of having fragmented insights into account usage and schema structure.

**Importance:** It matters in analytics and warehousing as it enables proactive resource optimization and cost management.

**Use Cases:**
- Monitoring data usage and storage
- Optimizing resource allocation
- Tracking query performance

### Typical Scenario
A data administrator wants to monitor the storage usage of tables in a specific schema and track query performance metrics to optimize resource allocation.

### Core Snowflake SQL Objects Used
`CREATE VIEW`

### Code Source Execution
```sql
CREATE VIEW schema_usage AS SELECT * FROM TABLE(INFORMATION_SCHEMA.TABLE_STORAGE_BY_SCHEMA()) AS t JOIN ACCOUNT_USAGE.STORAGE_BY_SCHEMA() AS s ON t.SCHEMA_NAME = s.SCHEMA_NAME;
```

### Example Execution Logic Step-by-Step
The example creates a view that combines metadata from Information Schema and ACCOUNT_USAGE. It joins two tables on the schema name: TABLE_STORAGE_BY_SCHEMA() from Information Schema provides schema metadata, while STORAGE_BY_SCHEMA() from ACCOUNT_USAGE provides storage metrics.

### Implementation Example
To implement this, first ensure that your role has the necessary privileges to access both Information Schema and ACCOUNT_USAGE. Then, create the view using the provided SQL code. Finally, query the view to retrieve the combined data.

### Explanation of the Code
- Step 1: Access Information Schema and ACCOUNT_USAGE
- Step 2: Join the tables on schema name
- Step 3: Create a view for easy querying

### When to Use
- Regularly monitoring account usage and schema changes
- Optimizing resource allocation based on usage patterns

### When Not to Use
- When detailed query performance metrics are not needed
- If only schema metadata is required without usage insights

### Common Mistakes
- Not granting sufficient privileges to access both Information Schema and ACCOUNT_USAGE
- Failing to refresh the view after schema changes

### Expected Output
**Description:** The result set includes columns from both Information Schema and ACCOUNT_USAGE, providing a comprehensive view of schema metadata and usage metrics.

**Schema:**
- SCHEMA_NAME
- TABLE_NAME
- STORAGE_USAGE
- QUERIES_EXECUTED

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*