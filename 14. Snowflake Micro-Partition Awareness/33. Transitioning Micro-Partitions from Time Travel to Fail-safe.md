# Transitioning Micro-Partitions from Time Travel to Fail-safe in Snowflake

**Description:**
This topic explains how to transition micro-partitions from Time Travel to Fail-safe in Snowflake, ensuring data protection and minimizing data loss. The process involves understanding the differences between Time Travel and Fail-safe, and executing the transition efficiently. By transitioning micro-partitions, users can leverage the benefits of both features.

## Short Explanation
**Overview:** Transitioning micro-partitions from Time Travel to Fail-safe allows for data protection and recovery.

**Problem Solved:** Data loss prevention during micro-partition transitions.

**Importance:** Ensures business continuity and minimizes data loss in Snowflake.

**Use Cases:**
- Disaster recovery
- Data protection and compliance

### Typical Scenario
A business needs to ensure that its Snowflake data is protected and recoverable in case of accidental deletions or updates. The company uses Time Travel for data recovery but wants to transition to Fail-safe for additional protection.

### Core Snowflake SQL Objects Used
`CREATE TABLE`, `ALTER TABLE`

### Code Source Execution
```sql
ALTER TABLE mytable SET DATA_RETENTION_TIME_IN_DAYS = 0; ALTER TABLE mytable SET FAILSAFE = 72;
```

### Example Execution Logic Step-by-Step
To transition micro-partitions from Time Travel to Fail-safe, first set the data retention time to zero, then enable Fail-safe with a specified duration (e.g., 72 hours).

### Implementation Example
CREATE TABLE mytable (id INT, name STRING); INSERT INTO mytable VALUES (1, 'John'); ALTER TABLE mytable SET DATA_RETENTION_TIME_IN_DAYS = 0; ALTER TABLE mytable SET FAILSAFE = 72;

### Explanation of the Code
- Step 1: Create a table and insert data.
- Step 2: Set data retention time to zero to transition from Time Travel.
- Step 3: Enable Fail-safe with a specified duration.

### When to Use
- When data loss is critical
- For regulatory compliance

### When Not to Use
- When data is temporary
- When data loss is not critical

### Common Mistakes
- Incorrect data retention settings
- Insufficient Fail-safe duration

### Expected Output
**Description:** Verify that the micro-partition has transitioned successfully.

**Schema:**
- name
- data_retention_time_in_days
- fail_safe

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*