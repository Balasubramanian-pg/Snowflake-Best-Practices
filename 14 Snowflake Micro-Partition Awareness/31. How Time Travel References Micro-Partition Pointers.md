# How Time Travel References Micro-Partition Pointers in Snowflake

**Description:**
This topic explains how Snowflake's Time Travel feature uses micro-partition pointers to enable efficient querying of historical data. Time Travel allows users to query data as it existed at a specific point in time, and micro-partition pointers play a crucial role in achieving this functionality. By understanding how Time Travel references micro-partition pointers, users can better optimize their queries and leverage the full potential of Snowflake's data management capabilities.

## Short Explanation
**Overview:** Snowflake's Time Travel feature uses micro-partition pointers to enable querying of historical data.

**Problem Solved:** Efficient querying of historical data without requiring extensive data duplication or manual data management.

**Importance:** Enables data analysts and scientists to analyze data as it existed at a specific point in time, which is critical in auditing, compliance, and data lineage.

**Use Cases:**
- Auditing and compliance
- Data lineage and provenance
- Historical data analysis

### Typical Scenario
A financial analyst needs to analyze the state of customer data as it existed on a specific date for auditing purposes. They use Snowflake's Time Travel feature to query the data as it existed at that point in time.

### Core Snowflake SQL Objects Used
`CREATE TABLE`, `ALTER TABLE`

### Code Source Execution
```sql
CREATE TABLE customers (id INT, name STRING, email STRING) CLUSTER BY (id);
ALTER TABLE customers SET DATA_RETENTION_TIME_IN_DAYS = 90;
SELECT * FROM customers VERSION = 1;
```

### Example Execution Logic Step-by-Step
In this example, the `customers` table is created with clustering on the `id` column. The `DATA_RETENTION_TIME_IN_DAYS` parameter is set to 90, which enables Time Travel for 90 days. The `SELECT` statement uses the `VERSION` function to query the data as it existed at a specific point in time.

### Implementation Example
To implement Time Travel with micro-partition pointers, create a table with clustering on one or more columns, and set the `DATA_RETENTION_TIME_IN_DAYS` parameter to a non-zero value. Then, use the `VERSION` or `TIMESTAMP` function to query the data as it existed at a specific point in time.

### Explanation of the Code
- Step 1: Create a table with clustering on one or more columns.
- Step 2: Set the `DATA_RETENTION_TIME_IN_DAYS` parameter to a non-zero value to enable Time Travel.
- Step 3: Use the `VERSION` or `TIMESTAMP` function to query the data as it existed at a specific point in time.

### When to Use
- Auditing and compliance
- Data lineage and provenance
- Historical data analysis

### When Not to Use
- Real-time data analysis
- High-performance transactional systems

### Common Mistakes
- Not setting `DATA_RETENTION_TIME_IN_DAYS` to a non-zero value
- Not using the `VERSION` or `TIMESTAMP` function to query historical data

### Expected Output
**Description:** The result set will contain the data as it existed at the specified point in time.

**Schema:**
- id
- name
- email

---
*Auto-enriched via Snowflake Studio | meta-llama/llama-4-scout-17b-16e-instruct*